---
name : Styled
menu : › Components
route: /components/styled
---

import { useState } from 'react';
import { Playground, Props } from 'docz';
import { Styled, Container, Row, Col, Card, Switch } from '../';

# Styled
### A simple component block which accepts custom props and styles.

.

<Playground component={Container} sm>
    <Styled
        background="supernova,crystal,0.5"
        color="supernova"
        padding="16px"
        radius="12px"
        shadow
        shadowColor="supernova,crystal"
        shadowSpread="24px"
    >
        Styled with custom props.
    </Styled>
</Playground>

.

## Simple example

<Playground>
    <Styled as="section">
        "Styled" renderized with "section" tag instead of "div".
    </Styled>
</Playground>

.

## Borders example

<Playground>
    <Styled
        borderTop
        borderBottom
        margin="32px 0"
        padding="16px"
    >
        Hello, borders.
        <br />
        Or should I'd say "shadows as borders"?!
    </Styled>

    <Styled
        borderTop
        borderRight="tangerine"
        borderBottom="ocean"
        borderLeft="ruby"
        margin="32px 0"
        padding="16px"
        radius={12}
    >
        Too many borders combined.
        <br />
        Check the 4 corners
    </Styled>
</Playground>

.

## Mixing styles example

<Playground>
    <Styled
        as={Container}
        background="mercury,crystal"
        padding="16px"
        radius={8}
        xs // this is a "Container" prop and you can use all others
    >
        "Styled" mixed up with "Container" component styles.
    </Styled>
</Playground>

.

## "Responsive" using `styles` and `stylesXS` or `stylesGTXS`

<Playground>
    <Styled
        shadow
        shadowColor="secondary,light"
        radius={12}
        padding={16}
        margin="32px 0"
        textColor="white"
        styles={{
            // General Styles
            background: 'blue',
        }}
        stylesXS={{
            // Styles to screens less than SM (767px or less)
            background: 'orange',
        }}
    >
        "Styled" renderized with "section" tag instead of "div".
    </Styled>
    <Styled
        margin="32px 0"
        padding={16}
        radius={24}
        textColor="white"
        styles={{
            // General Styles
            background: 'lightgreen',
        }}
        stylesGTXS={{
            // Styles to screens greater than XSM (476px or less)
            background: 'gray',
        }}
    >
        "Styled" renderized with "section" tag instead of "div".
    </Styled>
</Playground>

.

## Properties

<Props of={Styled} />
