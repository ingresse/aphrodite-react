---
name : Button
menu : › Components
route: /components/button
---

import { useState } from 'react';
import { Playground, Props } from 'docz';
import { colors } from '../../utils';
import { Button, Container, Segment, Row, Col } from '../';


# Button
### Button styled with Aphrodite definitions.

```jsx
import { Button } from 'aphrodite-react';

<Button>Hello, you</Button>
```


## Regular

<Playground>
    <Segment textAlign="center">
        <Button>
            Hello
        </Button>
    </Segment>
</Playground>


## Link

<Playground>
    <Segment textAlign="center">
        <Button link>
            Hello
        </Button>
    </Segment>
</Playground>


## Small

<Playground>
    <Segment textAlign="center">
        <Button small>
            Hello
        </Button>
    </Segment>
</Playground>


## Basic Colors

<Playground>
    <Row horizontal="center">
        <Col xs={12} sm={4}>
            <Segment padding="10px 20px" background={colors.get('info')}>
                <Button block margin="10px 0" color="black">
                    black
                </Button>
            </Segment>
        </Col>
        <Col xs={12} sm={4}>
            <Segment padding="10px 20px" background={colors.get('secondary')}>
                <Button block margin="10px 0" color="white">
                    white
                </Button>
            </Segment>
        </Col>
        <Col xs={12} sm={4}>
            <Segment padding="10px 20px" background={colors.get('success')}>
                <Button block margin="10px 0" color="smoke">
                    smoke
                </Button>
            </Segment>
        </Col>
    </Row>
</Playground>


## Colors Alias

<Playground>
    {() => {
        const colorsAlias = Object.keys(colors.alias);

        return (
            <section>
                <Segment padding="20px 0">
                    <Row horizontal="center" vertical="middle">
                        {colorsAlias.map((colorKey) => (
                            <Col xs={6} sm={4} md={4} lg={3} key={`color-${colorKey}`}>
                                <Button block margin="10px 0" color={colorKey}>
                                    {colorKey}
                                </Button>
                                <Button block small margin="10px 0" color={colorKey}>
                                    {colorKey}
                                </Button>
                            </Col>
                        ))}
                    </Row>
                </Segment>
                <Segment padding="20px 0">
                    <Row horizontal="center" vertical="middle">
                        {colorsAlias.map((colorKey) => (
                            <Col xs={6} sm={4} md={4} lg={3} key={`color-${colorKey}`}>
                                <Button link block margin="10px 0" color={colorKey}>
                                    {colorKey}
                                </Button>
                                <Button link block small margin="10px 0" color={colorKey}>
                                    {colorKey}
                                </Button>
                            </Col>
                        ))}
                    </Row>
                </Segment>
            </section>
        );
    }}
</Playground>


## Colors Shades

<Playground>
    {() => {
        const colorsShades   = Object.keys(colors.shades);
        const colorsExcluded = [
            'primary',
            'secondary',
            'info',
            'success',
            'warning',
            'error',
            'translucid',
        ];

        return (
            <Row horizontal="center">
                {colorsShades.map((colorKey) => {
                    if (colorsExcluded.includes(colorKey)) {
                        return (null)
                    }

                    return (
                        <Col xs={6} sm={4} md={4} lg={3} key={`color-${colorKey}`}>
                            <Button block margin="10px 0" color={colorKey}>
                                {colorKey}
                            </Button>
                            <Button block small margin="10px 0" color={colorKey}>
                                {colorKey}
                            </Button>
                        </Col>
                    );
                })}
            </Row>
        );
    }}
</Playground>


## Translucid

<Playground>
    <Row horizontal="center">
        <Col xs={12} sm={4} md={3}>
            <Segment padding="10px 20px" background={colors.get('success')}>
                <Button block margin="10px 0" color="translucid">
                    translucid
                </Button>
            </Segment>
        </Col>
    </Row>
</Playground>


## Customized Radius

<Playground>
    <Row horizontal="center">
        <Col xs={6} sm={3}>
            <Button block margin="10px 0">
                Regular Radius
            </Button>
        </Col>
        <Col xs={6} sm={3}>
            <Button block margin="10px 0" radius="12px">
                Custom Radius
            </Button>
        </Col>
        <Col xs={6} sm={3}>
            <Button block margin="10px 0" radius={6}>
                Custom Radius
            </Button>
        </Col>
        <Col xs={6} sm={3}>
            <Button block margin="10px 0" radius="0">
                Reset Radius
            </Button>
        </Col>
    </Row>
</Playground>


## Block

<Playground>
    <Button block>
        Block
    </Button>
</Playground>


## Custom Component

<Playground>
    {() => {
        const Span = (props) => (
            <span {...props} />
        );

        return (
            <Segment textAlign="center">
                <Button as={Span}>
                    Custom Component
                </Button>
            </Segment>
        );
    }}
</Playground>


## Loading State

<Playground>
    {() => {
        const [ btnText, setBtnText ] = useState('Load');
        const [ loading, setLoading ] = useState(false);

        function toggleLoading() {
            setLoading(!loading);

            setTimeout(() => {
                setBtnText('Done');
                setLoading(false);

                setTimeout(() => {
                    setBtnText('Again');
                }, 1000);
            }, 5000);
        }

        return (
            <Row horizontal="center">
                <Col xs={12} sm={6}>
                    <Segment textAlign="center">
                        <Button onClick={toggleLoading} loading={loading}>
                            {btnText}
                        </Button>
                    </Segment>
                </Col>
                <Col xs={12} sm={6}>
                    <Segment textAlign="center">
                        <Button small onClick={toggleLoading} loading={loading}>
                            {btnText}
                        </Button>
                    </Segment>
                </Col>
            </Row>
        );
    }}
</Playground>


## Disabled
Ignore `color` attribute

<Playground>
    <Segment textAlign="center">
        <Row horizontal="center">
            <Col xs={6} sm={4}>
                <Button disabled block color="success">
                    Regular
                </Button>
            </Col>
            <Col xs={6} sm={4}>
                <Button disabled block link color="success">
                    Link
                </Button>
            </Col>
            <Col xs={6} sm={4}>
                <Button disabled block translucid color="success">
                    Translucid
                </Button>
            </Col>
        </Row>
    </Segment>
</Playground>


## Properties
<Props of={Button} />
