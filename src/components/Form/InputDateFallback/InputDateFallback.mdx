---
name : Form | Input Date
menu : › Components
route: /components/form/input-date-fallback
---

import { memo, useState } from 'react';
import { Playground, Props } from 'docz';
import dayjs from 'dayjs';
import {
    InputDateFallback,
    Styled,
    Row,
    Col,
} from '../../';

# Form | Input Date (Fallback)
### A simple alternative to manage input date. It's used as fallback of DatePicker component.

This component works with [Day.js](https://day.js.org/) to validations and [react-input-mask](https://www.npmjs.com/package/react-input-mask) to mask pattern.

## Examples
<Playground>
    {() => {
        const [example, setExample] = useState('');

        return (
            <Row>
                <Col as={Styled} xs={12} sm={6} padding="10px">
                    <div>
                        Regular InputDateFallback
                    </div>
                    <br />
                    <InputDateFallback
                        id="example"
                        value={example}
                        onChange={({ target }) => { setExample(target.value); console.log('InputDateFallback target', target) }}
                        label="Type a date"
                    />
                    <br />
                    <div>
                        <strong>input value: </strong>
                        {JSON.stringify(example)}
                    </div>
                </Col>
            </Row>
        );
    }}
</Playground>


## Minimum Date
<Playground>
    {() => {
        const min                   = dayjs().add(30, 'days');
        const today                 = dayjs();
        const [example, setExample] = useState(today);

        return (
            <Row vertical="start">
                <Col as={Styled} xs={12} sm={6} padding="10px">
                    <div>
                        Minimum: {min.format('YYYY-MM-DD')}
                    </div>
                    <br />
                    <InputDateFallback
                        id="example2"
                        value={example}
                        onChange={({ target }) => setExample(target.value)}
                        label="Type a date"
                        min={min}
                    />
                    <br />
                    <div>
                        <strong>input value: </strong>
                        {JSON.stringify(example)}
                    </div>
                </Col>
            </Row>
        );
    }}
</Playground>


## Maximum Date
<Playground>
    {() => {
        const max                   = dayjs().subtract(30, 'days');
        const today                 = dayjs();
        const [example, setExample] = useState(today);

        return (
            <Row vertical="start">
                <Col as={Styled} xs={12} sm={6} padding="10px">
                    <div>
                        Maximum: {max.format('YYYY-MM-DD')}
                    </div>
                    <br />
                    <InputDateFallback
                        id="example3"
                        value={example}
                        onChange={({ target }) => setExample(target.value)}
                        label="Type a date"
                        max={max}
                    />
                    <br />
                    <div>
                        <strong>input value: </strong>
                        {JSON.stringify(example)}
                    </div>
                </Col>
            </Row>
        );
    }}
</Playground>


## Minimum and Maximum
<Playground>
    {() => {
        const [min, setMin] = useState(dayjs().subtract(2, 'days').format('YYYY-MM-DD'));
        const [max, setMax] = useState(dayjs().add(2, 'days').format('YYYY-MM-DD'));

        return (
            <Row vertical="start">
                <Col as={Styled} xs={12} sm={6} padding="10px">
                    <InputDateFallback
                        id="exampleFrom"
                        value={min}
                        onChange={({ target }) => setMin(target.value)}
                        label="From"
                        max={max}
                    />
                    <br />
                    <div>
                        <strong>input value: </strong>
                        {JSON.stringify(min)}
                    </div>
                    <div>
                        <strong>max: </strong>
                        {JSON.stringify(max)}
                    </div>
                </Col>
                <Col as={Styled} xs={12} sm={6} padding="10px">
                    <InputDateFallback
                        id="exampleTo"
                        value={max}
                        onChange={({ target }) => setMax(target.value)}
                        label="To"
                        min={min}
                    />
                    <br />
                    <div>
                        <strong>input value: </strong>
                        {JSON.stringify(max)}
                    </div>
                    <div>
                        <strong>min: </strong>
                        {JSON.stringify(min)}
                    </div>
                </Col>
            </Row>
        );
    }}
</Playground>


## Properties
<Props of={InputDateFallback} />
