---
name : Form
menu : Organisms
route: /organisms/form
---

import { useState } from 'react';
import { Playground, Props } from 'docz';
import { Input, Checkbox, Select, TextArea, NewSwitch, Card, Segment, Row, Col } from '../';

# Form
#### Simple form components

```jsx
import { Input, Checkbox, Select, TextArea } from 'aphrodite-react';
...
<Input type="text" ... />
```

---

## Switch
<Props of={NewSwitch} />

### Examples
<Playground>
    {() => {

        const [ col, setCol ]                 = useState(4);
        const [ text, setText ]               = useState('');
        const [ textAlign, setTextAlign ]     = useState(false);
        const [ textSmall, setTextSmall ]     = useState(false);
        const [ toggleAlign, setToggleAlign ] = useState(true);

        return (
            <Segment>
                <Row horizontal="center">
                    <Col xs={12} sm={col}>
                        <Card>
                            <NewSwitch
                                textSmall={textSmall}
                                textAlign={textAlign ? 'right' : 'left'}
                                toggleAlign={toggleAlign ? 'right' : 'left'}>
                                {text}
                            </NewSwitch>
                        </Card>
                    </Col>
                </Row>

                <Segment padding="40px 0">
                    <Row horizontal="center">
                        <Col xs={12} sm={12} md={9}>
                            <Segment padding="10px 0">
                                <Input
                                    type="text"
                                    label="Switch Label Text"
                                    placeholder="Type something..."
                                    onChange={(e) => setText(e.target.value)}
                                />
                            </Segment>
                        </Col>
                    </Row>

                    <Row horizontal="center">
                        <Col xs={6} sm={6} md={3}>
                            <Segment padding="10px 0">
                                <Checkbox
                                    id="switchToggleAlign"
                                    value={toggleAlign ? 'right' : 'left'}
                                    defaultChecked={toggleAlign}
                                    onChange={(e) => setToggleAlign(e.target.checked ? true : false)}>
                                    Align toggle at right
                                </Checkbox>
                            </Segment>
                        </Col>

                        <Col xs={6} sm={6} md={3}>
                            <Segment padding="10px 0">
                                <Checkbox
                                    id="switchTextAlign"
                                    value={textAlign ? 'right' : 'left'}
                                    defaultChecked={textAlign}
                                    onChange={(e) => setTextAlign(e.target.checked ? true : false)}>
                                    Align text at right
                                </Checkbox>
                            </Segment>
                        </Col>

                        <Col xs={6} sm={6} md={3}>
                            <Segment padding="10px 0">
                                <Checkbox
                                    id="switchTextSmall"
                                    value={textSmall ? true : false}
                                    defaultChecked={textSmall}
                                    onChange={(e) => setTextSmall(e.target.checked ? true : false)}>
                                    Text small
                                </Checkbox>
                            </Segment>
                        </Col>
                    </Row>
                </Segment>
            </Segment>
        );
    }}
</Playground>

---

## Input
<Props of={Input} />

### Examples
<Playground>
    <Row>
        <Col xs={6} styles={{ margin: '10px 0' }}>
            <Checkbox
                type="checkbox"
                id="ExampleCheckbox">
                regular checkbox
            </Checkbox>
        </Col>
        <Col xs={6} styles={{ margin: '10px 0' }}>
            <Checkbox
                id="ExampleCheckboxRight"
                right>
                aligned checkbox
            </Checkbox>
        </Col>
    </Row>
</Playground>

<Playground>
    <Input
        type="text"
        id="ExampleInputWithoutPlaceholder"
        label="Field Label"
    />
</Playground>

<Playground>
    <Input
        type="text"
        id="ExampleInputWithPlaceholder"
        placeholder="Field Placeholder"
    />
</Playground>

<Playground>
    <Input
        type="text"
        id="ExampleInput"
        label="Field Label"
        placeholder="Field Placeholder"
    />
</Playground>

<Playground>
    <Input
        type="text"
        id="ExampleInputError"
        value="With error"
        label="Field Label"
        placeholder="Field Placeholder"
        error
    />
</Playground>

<Playground>
    <Input
        type="text"
        id="ExampleInputErrorWithMsg"
        value="With error"
        label="Field Label"
        placeholder="Field Placeholder"
        error
        errorMessage="And with error message"
    />
</Playground>

<Playground>
    <Input
        type="text"
        id="ExampleInputErrorWithMsg"
        value="With error"
        label="Field Label"
        placeholder="Field Placeholder"
        error
        errorMessage="And with error message"
        disabled
    />
</Playground>

---

## Select
<Props of={Select} />

### Examples
<Playground>
    <Select label="Select Label"
    >
        <option value=""></option>
        <option value="ingresse">Ingresse</option>
        <option value="aphrodite">Aphrodite</option>
        <option value="react">React</option>
    </Select>
</Playground>

---

## TextArea
<Props of={TextArea} />

### Examples
<Playground>
    <TextArea
        id="ExampleTextAreaWithoutPlaceholder"
        label="TextArea Label">
    </TextArea>
</Playground>
