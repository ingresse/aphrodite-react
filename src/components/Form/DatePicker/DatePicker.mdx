---
name : Form | DatePicker
menu : › Components
route: /components/form/date-picker
---

import { memo, useState } from 'react';
import { Playground, Props } from 'docz';
import dayjs from 'dayjs';
import {
    DatePicker,
    Styled,
    Row,
    Col,
} from '../../';

# Form | DatePicker
### Uses browser's datepicker and do some validations. InputDateFallback when Safari Desktop.

This component works with [Day.js](https://day.js.org/), which is a project dependency.

## Examples
<Playground>
    {() => {
        const [example, setExample] = useState();

        return (
            <Row>
                <Col as={Styled} xs={12} sm={6} padding="10px">
                    <div>
                        Regular DatePicker
                    </div>
                    <br />
                    <DatePicker
                        id="example"
                        value={example}
                        onChange={({ target }) => setExample(target.value)}
                        label="Pick a date"
                    />
                    <br />
                    <div>
                        <strong>input value: </strong>
                        {JSON.stringify(example)}
                    </div>
                </Col>
            </Row>
        );
    }}
</Playground>


## Minimum Date
<Playground>
    {() => {
        const min                   = dayjs().add(30, 'days');
        const today                 = dayjs();
        const [example, setExample] = useState(today);

        return (
            <Row vertical="start">
                <Col as={Styled} xs={12} sm={6} padding="10px">
                    <div>
                        Minimum: {min.format('YYYY-MM-DD')}
                    </div>
                    <br />
                    <DatePicker
                        id="example2"
                        value={example}
                        onChange={({ target }) => setExample(target.value)}
                        label="Pick a date"
                        min={min}
                    />
                    <br />
                    <div>
                        <strong>input value: </strong>
                        {JSON.stringify(example)}
                    </div>
                </Col>
            </Row>
        );
    }}
</Playground>


## Maximum Date
<Playground>
    {() => {
        const max                   = dayjs().subtract(30, 'days');
        const today                 = dayjs();
        const [example, setExample] = useState(today);

        return (
            <Row vertical="start">
                <Col as={Styled} xs={12} sm={6} padding="10px">
                    <div>
                        Maximum: {max.format('YYYY-MM-DD')}
                    </div>
                    <br />
                    <DatePicker
                        id="example3"
                        value={example}
                        onChange={({ target }) => setExample(target.value)}
                        label="Pick a date"
                        max={max}
                    />
                    <br />
                    <div>
                        <strong>input value: </strong>
                        {JSON.stringify(example)}
                    </div>
                </Col>
            </Row>
        );
    }}
</Playground>


## Minimum and Maximum
<Playground>
    {() => {
        const [min, setMin] = useState(dayjs().subtract(2, 'days').format('YYYY-MM-DD'));
        const [max, setMax] = useState(dayjs().add(2, 'days').format('YYYY-MM-DD'));

        return (
            <Row vertical="start">
                <Col as={Styled} xs={12} sm={6} padding="10px">
                    <DatePicker
                        id="exampleFrom"
                        value={min}
                        onChange={({ target }) => setMin(target.value)}
                        label="From"
                        max={max}
                    />
                    <br />
                    <div>
                        <strong>input value: </strong>
                        {JSON.stringify(min)}
                    </div>
                </Col>
                <Col as={Styled} xs={12} sm={6} padding="10px">
                    <DatePicker
                        id="exampleTo"
                        value={max}
                        onChange={({ target }) => setMax(target.value)}
                        label="To"
                        min={min}
                    />
                    <br />
                    <div>
                        <strong>input value: </strong>
                        {JSON.stringify(max)}
                    </div>
                </Col>
            </Row>
        );
    }}
</Playground>


## Properties
<Props of={DatePicker} />
