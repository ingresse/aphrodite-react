---
name : Segment
menu : Atoms
route: /atoms/segment
---

import { useState } from 'react';
import { Playground, Props } from 'docz';
import { Segment, Container, Row, Col, Card, Switch } from '../';
import { RADIUS } from '../../constants';

# Segment
#### A simple block, with default paddings of `20px 10px`.

```jsx
import { Segment } from 'aphrodite-react';

...

<Segment>
    Hello
</Segment>
```

## Properties
<Props of={Segment} />

## Examples
<Playground style={{ padding: '20px' }}>
    {() => {
        const [ hasCustomStyles, setHasCustomStyles ] = useState(true);

        return (
            <Container>
                <Row horizontal="center">
                    <Col xs={12} sm={6}>
                        <Segment>
                            Default Segment
                        </Segment>

                        <Segment color="supernova">
                            Segment with custom color prop
                        </Segment>

                        <Segment
                            textUpper={hasCustomStyles}
                            radius={hasCustomStyles ? 10 : 0}
                            color={hasCustomStyles ? 'inverse' : ''}
                            background={hasCustomStyles ? 'success' : ''}>
                            <div>
                                This is a `Segment`.
                            </div>
                            A simple block to segmentate all the things.
                        </Segment>

                    </Col>
                </Row>

                <Segment padding="40px 0 10px">
                    <Row horizontal="center">
                        <Col xs={12} sm={6}>
                            <Card>
                                <Switch
                                    value={hasCustomStyles}
                                    defaultChecked={hasCustomStyles}
                                    onChange={() => setHasCustomStyles(!hasCustomStyles)}>
                                    Display with custom styles.
                                </Switch>
                            </Card>
                        </Col>
                    </Row>
                </Segment>
            </Container>
        );
    }}
</Playground>
