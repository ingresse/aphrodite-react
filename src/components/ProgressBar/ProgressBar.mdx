---
name : Progress Bar
menu : › Components
route: /components/progress-bar
---

import { Playground, Props } from 'docz';
import { ProgressBar, Segment } from '../';
import { colors } from '../../utils';


# Progress Bar
```jsx
import { colors, ProgressBar } from 'aphrodite-react';

<ProgressBar
    size="200px"    // Have priority
    percentual={60} // Will be used if "size" is inexistent
    animated={true}
    gradient={{ start: colors.get('tangerine'), end: colors.get('tangerine', 'dark') }}
    width={460}
/>
```


## Examples

<Playground>
    <Segment padding="10px 0">
        <ProgressBar
            percentual={20}
        />
    </Segment>
    <Segment padding="10px 0">
        <ProgressBar
            percentual={40}
            height={20}
            color="supernova"
        />
    </Segment>
    <Segment padding="10px 0">
        <ProgressBar
            percentual={60}
            height={30}
            color={'red'}
        />
    </Segment>
    <Segment padding="10px 0">
        <ProgressBar
            percentual={80}
            height={40}
            animated={true}
            gradient={{
                start: colors.get('warning'),
                end  : colors.get('error'),
            }}
        />
    </Segment>
    <Segment padding="10px 0">
        <ProgressBar
            percentual={100}
            height={50}
            radius={25}
            animated={true}
            gradient={{
                start: colors.get('bamboo', 'crystal'),
                end  : colors.get('bamboo', 'dark'),
            }}
        />
    </Segment>
</Playground>


## Properties

<Props of={ProgressBar} />
