/* Theme Props */
import colors from './docs.colors';

/* Theme Settings */
const theme = {
    /**
     * Colors
     */
    colors,

    /**
     * Typography
     */
    fonts: {
        code : 'monospace',
        body : '"Roboto", sans-serif',
        title: '"Roboto", sans-serif',
    },
};

export default theme;
