/* Theme Props */
import colors from './docs.colors';

/* Theme Settings */
const theme = {
    /**
     * Colors
     */
    colors,

    /**
     * Typography
     */
    fonts: {
        mono   : 'monospace',
        display: '"Roboto", sans-serif',
        ui     : '"Roboto", sans-serif',
    },
};

export default theme;
